schema: space-widget/v1
id: midi-jazz-practice
name: MIDI Jazz Practice
cols: 8
rows: 6
col: 0
row: 18
metadata:
  example: true
  suite: jcn-command-center
  purpose: "Web MIDI chord detection, voicing capture, and jazz practice."
renderer: |-
  async (parent, currentSpace, context) => {
    const center = await context.import("scripts/command-center.js")
    return center.render(parent, currentSpace, context, "midi-jazz-practice")
  }
