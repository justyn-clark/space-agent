schema: space-widget/v1
id: audio-asset-inspector
name: Audio Asset Inspector
cols: 8
rows: 6
col: 16
row: 12
metadata:
  example: true
  suite: jcn-command-center
  purpose: "Drop-in waveform, loudness, and audio-tool command inspector."
renderer: |-
  async (parent, currentSpace, context) => {
    const center = await context.import("scripts/command-center.js")
    return center.render(parent, currentSpace, context, "audio-asset-inspector")
  }
