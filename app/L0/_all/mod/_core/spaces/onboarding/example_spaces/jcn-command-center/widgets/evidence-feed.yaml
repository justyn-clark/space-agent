schema: space-widget/v1
id: evidence-feed
name: Evidence Feed
cols: 8
rows: 6
col: 0
row: 6
metadata:
  example: true
  suite: jcn-command-center
  purpose: "Verification receipts, checks, screenshots, and proof trails."
renderer: |-
  async (parent, currentSpace, context) => {
    const center = await context.import("scripts/command-center.js")
    return center.render(parent, currentSpace, context, "evidence-feed")
  }
